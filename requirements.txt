# 必须 - GUI
PySide6>=6.6.0

# 必须 - 真实浏览器自动化 (替代 QWebEngine)
playwright>=1.40.0
# 装完之后跑一次:python -m playwright install chromium

# 可选 - 真实无水印解析 (缺失则 fallback 到 mock)
# git clone https://github.com/ihmily/doubao-nomark
# cd doubao-nomark && pip install -e .
