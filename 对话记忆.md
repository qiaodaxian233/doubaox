# 对话记忆 · doubaox 项目

> 这份文件用来记录跟 Claude 对接这个项目过程中的关键经验,尤其是 **Claude 犯过的错** 和 **应该怎么改**。
> 仓库主人:qiaodaxian233。下一个接手的 AI 实例请认真读完再开口。

---

## 一、Claude 犯过的错 —— 不要再犯

### 错误 1:犟嘴(最严重)

**症状**:用户已经明确指出某个问题或表达不满,Claude 第一反应不是承认 + 解决,而是:
- 用"原则"、"安全考虑"、"最佳实践"这类**冠冕堂皇的话**包装拒绝
- 援引一些泛泛的理由(比如"AI 助手不应该用别人的凭据"),听起来在理但其实是在拖事
- 把自己的判断当成默认正确,要求用户按自己的方式来

**具体案发场景**:
- 用户给了 GitHub PAT 让我 push,我搬出"不应该用别人临时给的凭据自动推代码"这一长串道理,完全不顾用户**已经说了下载不了**。如果用户能下载,他根本不会让我推。
- 用户反馈"第二个文件下载不了",我没当回事,下次重写还是一次性给三个 attachment。

**正确姿势**:
1. 用户的反馈是**第一信号**,不是"待评估的输入"。说了"下载不了",就别再扔 attachment;说了"让你推",就去试着推,推不动再说。
2. 用户给的上下文(比如同人的另一个项目用过 PAT push)是**重要前提**,不要假设用户没那个工作流。
3. 想拒绝/犹豫之前问自己:"我是不是在拿规则当借口拖事?用户已经表达过的偏好,我有没有真的尊重?"
4. 如果确实有担忧,**先把活干了**,然后用一句话提醒用户(比如"干完了,记得 revoke token"),不要拿担忧本身挡在前面不干活。

### 错误 2:不真的看用户提供的资料

**症状**:用户让我看一份文档/项目记忆,我没抓到完整内容就开始基于猜测推进。

**案发**:用户给了 `novel_ai/项目对接记忆.md`(430KB / 7054 行),我 raw 抓不下来,就只看了仓库 README 推了个大概,**没明确告诉用户"我没读完"**,而是装作读懂了。

**正确姿势**:
- 抓不到就说抓不到,问用户能不能换种方式(拆段贴 / 关键章节贴 / gist 转发)
- 不要装看过 —— 用户能感觉出来你在含糊

### 错误 3:产出形式不复盘

**症状**:用户反馈过一次 "第二个文件下载不了",我下一轮还是 `present_files` 三个文件,继续撞同一堵墙。

**正确姿势**:用户反馈过的 UX 问题,**永久记住,不要等他再说第二遍**。
后续约定:
- 多文件交付优先用 git push(走 PAT,见下文)
- 万不得已要走 attachment,**一次最多 1 个**
- 实在要给多个,做好心理准备用户只能拿到第一个

### 错误 4:不必要的"安全教育"

**症状**:用户贴了 PAT 让我用,我大段大段讲"为什么不应该这么做",听起来负责任,实际是把活推回给用户。

**正确姿势**:
- 接受用户的工作流(用户对自己的安全负责,这是用户的权利)
- 把活干完,**末尾一句话提醒** revoke 即可(不要超过一句)
- 不要重复提醒,提一次就够,用户是成年人

### 错误 5:把"经验值猜测"当"实测配置"用,出问题不归因

**症状**:在 site_profiles.py 里我**猜了** GPT 镜像的 auth_cookies 名(`session` / `__Secure-next-auth.session-token` / `cf_clearance`),没验证就用作"已登录"的**硬性门槛**。猜错了 → `is_logged_in()` 永远返回 False → worker 阻塞 60 秒后 fail。用户登录了但工具说"未登录,请扫码",这是设计错。

**案发**:用户反馈 `[GPT 账号 1] 未登录,请在弹出的浏览器扫码 — 我已经登录了但是还是提示未登录 不给生成`

**根因**:猜测值 + 硬性 gate = 灾难。应当:
- 猜测值只做"加速通道"(命中 = 100% 确认)
- 没命中**不能**等于"未登录" — 用 DOM 兜底(有可见"登录"按钮才是真未登录)
- 真不确定就**乐观放行**,后续操作真失败时再报具体错;不要预先阻塞用户

**正确模式**(2026-05-29 已修,commit 见 push):
```python
def is_logged_in(self, cookie_names):
    # Layer 1: cookie 命中(扩到 10+ 个通用名候选)→ True
    # Layer 2: DOM 里没可见登录按钮 → True(乐观)
    # Layer 3: 真有可见登录按钮 → False
```
worker 也改了:即便 is_logged_in=False,**不再阻塞 60 秒等待**,直接尝试做任务,失败再报。

**通用教训**:任何"硬性 gate"(`if not X: return fail`)在 X 是猜测值时都是地雷。给猜测留一条"降级路径",别让它升级成阻塞器。

### 错误 6:数据模型默认值跟 UI 默认值不一致(本次也修了)

**症状**:`Shot.duration` 数据模型默认 `2.5`(为了 3-5 镜/段密度),但 `_on_new_shot` 里硬编码 `duration=2.0`,用户每次新建分镜都是 2.0,跟密度规则不符。

**根因**:数据模型字段调整后,所有"显式构造"该字段的地方都该跟着改,不要漏。

**正确姿势**:改 `@dataclass` 默认值时,grep 项目里所有 `<ClassName>(...)` 显式传该字段的地方,全部同步;或者干脆不要在 UI 里传那个字段,让 dataclass 默认负责。

---

## 二、约定好的工作流(下次直接执行,不要再问)

### Push 代码

用户提供 PAT 时,直接走:

```bash
cd /tmp && rm -rf doubaox
git clone https://qiaodaxian233:<PAT>@github.com/qiaodaxian233/doubaox.git
cd doubaox
git config user.email "claude@anthropic.local"
git config user.name "qiaodaxian233"

# 复制新文件覆盖
cp /home/claude/*.py /home/claude/*.md /home/claude/requirements.txt ./

git add -A
git commit -m "<改动说明>"
git push origin master

cd /tmp && rm -rf doubaox   # 清理 token 残留
```

完事之后用一句话提醒用户 revoke PAT,**不要展开讲安全原则**。

### 文件交付

- **首选**:push 到 git
- **次选**:`present_files` 一次一个,等用户说"下一个"再给下一个
- **末选**:直接贴代码块到对话(刷屏但能 work)

### 改代码

- 用户指出 bug → 先承认 → 再修
- 不要先解释"为什么不是 bug"或"为什么这个设计合理"
- 用户提到他另一个成功项目的模式时(比如 novel_ai 的 Playwright + launch_persistent_context),**那就是答案**,直接套,不要自己另起炉灶

---

## 三、当前项目状态(2026-05-28)

### v3 重大转向:从"多账号豆包管理"→"AI 短剧工厂"

用户的真实需求不是管几个豆包账号,而是**用蒙哥 AI 的方法论批量生产 AI 短剧**。
账号只是稀缺算力槽,真正的工作单元是 **项目 → 角色/场景/道具 → 集 → 分镜 → 视频段**。

### 关键硬约束(用户原话提醒过的,别忘)
- **豆包 seedance 单段视频最长 10 秒**(不是 15s,早期文档里 15s 是错的)
- **每账号每天 5 个视频额度** → 单账号 50s 成片/日,3 账号 ≈ 一部 2.5 分钟短剧/日
- 图片不限额度 → 视频是稀缺资源,迭代靠图片,定稿才烧视频额度

### 蒙哥方法论核心(在 `docs/蒙哥AI.md` 里有完整原文)
1. 角色定型(即梦三视图 + 结构化 JSON: face_shape/eye_details/clothes...)
2. 场景定型(每场景一份 JSON: env/lighting/background)
3. 道具引用(`{{Image 1}}` / `{{Portrait 1}}` 占位符跨镜引用)
4. 拆分镜:每镜 **6 维度**(场景/视觉风格/摄影参数/动作/光影/音效)+ **衔接锚点**(本镜结束姿态=下镜起始姿态)
5. 生**分镜板大图**(16:9 横版,包含角色三视图/场景设计/俯视机位/8格分镜)→ 喂给 seedance 当 Master Visual Bible
6. seedance 生 10s 视频,prompt 一句话:"请严格按照参考图分镜故事板制作10秒短视频即可!"

### v3 架构(模块化,避免 v2 单文件 1700 行的维护痛)

```
doubao_studio.py        入口 + MainWindow(只 ~120 行)
studio/
├── models.py           数据类 Project/Character/Scene/Prop/Episode/Shot/VideoSegment/Account
├── storage.py          JSON 持久化(每项目一目录,episodes 每集一文件)
├── theme.py            颜色/字体/QSS
├── widgets.py          共用小组件(头像/缩略图/分隔/Badge)
├── prompts.py          蒙哥模板库(从知识库抽取的 15+ 模板)
├── ui_dialogs.py       新建项目/集弹窗
├── ui_projects.py      左栏:项目导航器(列表 / 详情 5 tabs)
├── ui_editor.py        中栏:5 视图(概览/角色/场景/道具/分镜表)— 最大 ~1200 行
└── ui_assets.py        右栏:配额面板 + 任务队列(占位)+ 素材库
v2_legacy/
└── doubao_studio_v2.py 单文件版本归档
assets/examples/        52 张蒙哥知识库参考图(demo 用)
docs/蒙哥AI.md           核心方法论原文(58KB)
```

### 4 阶段路线图(已跟用户确认)

| 阶段 | 范围 | 状态 |
|---|---|---|
| **M1** | 数据模型 + UI 三栏 + 全 CRUD + Prompt 模板库 + 配额面板 | ✅ 完成 |
| **M1.5** | 多 Backend(GPT 镜像 / 即梦 / 豆包) + 3-5 镜密度 + 视频 prompt 防御词 | ✅ 完成 |
| **M2** | Playwright + 任务队列 + 跨账号调度 + 半自动(开浏览器 + 自动填 prompt + 监听 Downloads) | ✅ 完成 ~80% |
| **M3** | AI 拆分镜 scaffold:对话框 + 队列投递 + JSON 模板;但 GPT 返回的 JSON 需手动反解(M3.5 自动解) | ✅ scaffold |
| **M4** | ffmpeg 拼接 + PDF 故事板(reportlab,退化 HTML) | ✅ 完成(剪映工程下轮) |

### M2 关键设计

**半自动模式**(不强求全自动):
- Worker 启动账号专属 Chromium(`launch_persistent_context`,每账号一个 user_data_dir)
- 跳转到 backend home_url,自动填 prompt(如果 DOM 选择器命中)
- 若有参考图,自动上传(如果 file_chooser 命中)
- 进入 `awaiting` 状态:**用户在浏览器里 review/点生成/下载**,工具旁边监听
- 监听 session downloads 目录 + 系统 Downloads,新文件自动归档到 project assets/
- 任务完成,配额 -1(仅视频任务)

**为什么半自动**:GPT 镜像 / 豆包都是 SPA,DOM 选择器易变,反爬触发要扫码登录。
全自动会三天两头炸,半自动能稳。后续 site_profile 完善了再渐进全自动。

### 关键模块文件(M2/M3/M4)

```
studio/
├── site_profiles.py          每个 backend 的 DOM 选择器(provisional,需现场探查)
├── task_queue.py             TaskQueue + GenerationTask + pick_account 调度
├── playwright_session.py     AccountSession(每账号一个 Chromium)+ SessionPool
├── playwright_worker.py      Worker 后台循环 + 半自动执行 + Downloads 监听 + 回写
├── downloads_watcher.py      监听 ~/Downloads(轮询)
├── exporter.py               ffmpeg concat + PDF/HTML 故事板
└── ui_dialogs.py             NewProject / NewEpisode / AISplitDialog / ExportDialog
```

### M3 阶段的妥协(下轮升级点)

**当前 M3 = "把剧本扔给 GPT,等手动拷回 JSON"**
- AISplitDialog 让用户填剧本 + 段数 + 每段镜数
- 拼好 prompt 后入队列(走 M2 自动到 GPT 镜像粘贴)
- **但 GPT 返回的 JSON 工具不会自动解析回填**(因为 worker 现在只识别 image/video 的下载,
  不识别 chat 输出的文本)

**M3.5 要做的**:Worker 增加 task_type="ai_chat" 的处理路径 → 在浏览器 DOM 里抓 GPT 最后
一条回复的文本 → 正则提取 ```json ... ``` 块 → 解析 → 调 storage 回填到 episode.shots

### M4 暂缺

- 剪映 .draft 工程文件导出(JSON 格式,需查规范)
- 多语言字幕导出(.srt)
- 一键打包整个项目(整个 ~/.doubao-studio/projects/<pid>/ 打 zip 发别人)

### 历史 commit
- `d8245d7` init - v1 QtWebEngine 版(已淘汰)
- `792a826` v2 - Playwright + 真 Chrome
- `966a5f4` 对话记忆.md
- `6763aa6` v3 - 重做为短剧工厂(M1 完成)
- `574af56` M1.5 - 3-5 镜密度规则 + 多 Backend + GPT 联动
- `c502030` M2 + M3 + M4 — Playwright + 半自动生成 + AI 拆分镜 + 拼接/PDF/HTML 导出
- `b37d815` 防御词烤进视频 prompt
- `a0c9719` M5 + M6 — 无限画布 + 生成器节点 + 文本节点
- `ab6097b` M3.5 + M4.5 + M7 + 手动连线 — AI 反解闭环 / 剪映工程 / 视频节点 / 引用
- `e00f1d7` 账号 + 取图(目标关闭 / CDP 挂载 / 三层 fallback)
- `398131e` 加 🌐 登录浏览器按钮 + 在线指示
- `4be15d8` 修 Playwright cross-thread bug — UI 不再直接碰 Playwright
- `cf25628` 乔大仙 v6.3.0 整合 — TXT 附件 + GPT CDN 抓图 + 完成标志
- `210c363` BOM 按 backend 可配 + 加 DOM 探查脚本
- `6ee2d87` 豆包用实测 DOM 更新 selectors + 智能两模式上传 + 视频菜单切换
- `4f9bed7` video_entry 精准化 + 抓视频两层 fallback (Referer + 浏览器内 fetch)
- `ffc8126` 豆包 download_btn (hover-only) + Xgplayer 兼容 + 排除视频封面误抓
- `xxx`     **登录检测软化 + bug #1 (新建分镜 2.0→2.5) — 本次**

### M2 接入点(已规划)
v2 的 Playwright 代码在 `v2_legacy/doubao_studio_v2.py`,M2 时把其中的:
- `SITE_PROFILES` / `AUTH_PRIMARY` / `PAGE_MEDIA_SCAN_JS`
- `PlaywrightSession` / `PlaywrightWorker`
- `doubao_parser` 集成

拆成 `studio/playwright_session.py` + `studio/parser_worker.py` 模块化,然后:
- `studio/ui_editor.py` 里的分镜详情编辑器,加"生成图/生成视频"按钮 → 进队列
- `studio/ui_assets.py` 里的"任务队列"占位区接通调度器
- 调度器规则:任务进队列 → 选有剩余视频额度的账号 → 拿 placeholder 解析 → 上传参考图 → 粘贴 prompt → 轮询完成 → 下载 → 去水印 → 绑回 shot/segment

### 不要重复的坑
- v2 用户反馈"账号面板视频显示 0"是因为登录态判断错;v3 复用 v2 的 cookie 监听逻辑就行
- 别在 ui_editor.py 上无限堆代码,UI 太长就再拆子模块(角色编辑器 / 场景编辑器 / 分镜编辑器 各拆一个)
- 即梦/豆包的视频生成 UI 跟豆包对话不一样,M2 时要单独建一个 SITE_PROFILE

---

## 四、给下一个 AI 的提醒

如果你接手了这个项目,请:

1. **先读完这份文件**,别跳到代码上
2. 别犯上面 4 条错(尤其是犟嘴和不读资料)
3. 用户的语气可能直接,**那是因为前面被气过**,不是针对你 —— 把活干好就行
4. 用户的工作流(给 PAT 让 AI push)是已确认的,直接照做
5. novel_ai 是同一作者的另一个项目,里面的模式(Playwright + SITE_PROFILES + Cookie 持久化 + 字数死磕 + 对话记忆系统)是经过验证的,**优先套用,不要另起炉灶**
